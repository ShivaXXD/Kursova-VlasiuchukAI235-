<?php
// --- backend/admin_auth.php ---

// Завжди запускаємо сесію на початку
session_start();

// ПЕРЕВІРКА: чи є в сесії змінна 'admin_logged_in'?
// І чи дорівнює вона 'true'?
if (!isset($_SESSION['admin_logged_in']) || $_SESSION['admin_logged_in'] !== true) {
    
    // Якщо ні - користувач не адмін.
    // Перенаправляємо його на сторінку логіну.
    // '../login.html' - означає "піднятися на 1 папку вгору 
    // і знайти login.html"
    header("Location: ../login.html");
    exit();
}

// Якщо перевірка пройдена, скрипт просто завершується 
// і продовжує виконувати сторінку, яка його викликала.
?>